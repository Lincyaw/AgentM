# core-manifest.yaml
version: 1

constitution:
  paths:
    # SDK kernel: mechanism and public ABI.
    - src/agentm/core/abi/**
    - src/agentm/core/lib/**
    - src/agentm/core/runtime/**
    - src/agentm/core/observability/**
    # Extension contract mechanism.
    - src/agentm/extensions/validate.py
    # The manifest itself — self-referential lock
    - core-manifest.yaml

managed:
  globs:
    - src/agentm/extensions/builtin/**.py
    - .claude/**.md
    - "*.md"

extension_api:
  # 2: removed the ContextEvent channel, the ToolCallEvent "rewrite" return,
  # and ExtensionManifest.priority. All three are breaking for atoms.
  current: 2
  semver_rules:
    major: "Breaking change to ExtensionAPI signatures, event payloads, or hook semantics"
    minor: "New event/method added; existing surface backward compatible"
    patch: "Implementation only; no API change"
  deprecation:
    # An atom written for api_version=N is loadable on N..N+1; rejected on N+2.
    grace: 1

reload:
  # Hot reload is not part of the reduced SDK core yet.
  tier_2_atoms: []
