# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.pytest_cache/

# Distribution / packaging
dist/
build/
*.egg-info/
*.egg

# Virtual environments
.venv/
venv/
env/

# uv
.uv/
.coverage
*.db*
trajectories
*.log

# Environment variables (contains secrets)
.env
.env.local
eval-data
logs
debug
eval-trajectories

# Runtime artifacts
.agentm/
.harness/
trajectory.jsonl
*_trajectory.jsonl

# Subagent worktrees
.claude/worktrees/

# Local datasets (downloaded benchmarks; large)
datasets/

# Ephemeral planning/task artifacts (kept locally, not shipped)
.claude/tasks/
.claude/plans/

# Transient TUI verification captures. Curated captures live under
# .agent/tui-dev/<topic>/ and stay explicitly tracked when useful.
.agent/tui-dev/captures/

# llmharness local experiment outputs (audit dumps, eval DBs, per-iter runs)
contrib/extensions/llmharness/runs/
runs/

# local eval/benchmark job outputs (terminal_bench harbor runs, litellm batches)
jobs/

evolution_runs

.agent