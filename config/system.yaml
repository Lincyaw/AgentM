models:
  gpt-5.1:
    api_key: "${AGENTM_API_KEY}"
    base_url: "${AGENTM_API_BASE_URL:}"
    rate_limit:
      requests_per_second: 20
      max_bucket_size: 50
  gpt-5.1-mini:
    api_key: "${AGENTM_API_KEY}"
    base_url: "${AGENTM_API_BASE_URL:}"
    rate_limit:
      requests_per_second: 20
      max_bucket_size: 50
  MiniMax-M2.5:
    api_key: "${AGENTM_API_KEY}"
    base_url: "${AGENTM_API_BASE_URL:}"
    rate_limit:
      requests_per_second: 20
      max_bucket_size: 50
  GLM-5:
    api_key: "${AGENTM_API_KEY}"
    base_url: "${AGENTM_API_BASE_URL:}"
    rate_limit:
      requests_per_second: 20
      max_bucket_size: 50
  ep-20250919144417-j2j4b:
    api_key: "${AGENTM_API_KEY}"
    base_url: "${AGENTM_API_BASE_URL:}"
    rate_limit:
      requests_per_second: 20
      max_bucket_size: 50
  claude-sonnet-4-6:
    api_key: "${AGENTM_API_KEY}"
    base_url: "${AGENTM_API_BASE_URL:}"
    provider: "anthropic"
    rate_limit:
      requests_per_second: 20
      max_bucket_size: 50
  glm-5:
    api_key: "${AGENTM_API_KEY}"
    base_url: "${AGENTM_API_BASE_URL:}"
    rate_limit:
      requests_per_second: 20
      max_bucket_size: 50
  deepseek-v3.2:
    api_key: "${AGENTM_API_KEY}"
    base_url: "${AGENTM_API_BASE_URL:}"
    rate_limit:
      requests_per_second: 20
      max_bucket_size: 50
  claude-haiku-4-5-20251001:
    api_key: "${AGENTM_API_KEY}"
    base_url: "${AGENTM_API_BASE_URL:}"
    provider: "anthropic"
    rate_limit:
      requests_per_second: 20
      max_bucket_size: 50
  Doubao-Seed-2.0-pro:
    api_key: "${AGENTM_API_KEY}"
    base_url: "${AGENTM_API_BASE_URL:}"
    rate_limit:
      requests_per_second: 20
      max_bucket_size: 50 

storage:
  checkpointer:
    backend: "sqlite"
    url: "./checkpoints.db"
  store:
    backend: "memory"
    url: ""

recovery:
  mode: "manual"
  expose_api: true

debug:
  trajectory:
    enabled: true
    output_dir: "./trajectories"
  console_live: false
  verbose: false
