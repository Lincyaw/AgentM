# AgentM Core Abstraction Inventory

This document separates fixed SDK/core contracts from implementation backlog.
The refactor target is that future work implements Protocol backends and policy
atoms without reopening trajectory, store, or cancellation modeling.

## Boundary Rule

Do not add optional backends directly into minimal SDK core unless the backend
is required for embedded SDK sessions to run.

Core defines stable ports, invariants, service keys, and default local
behavior. Optional packages provide durable storage, remote execution,
gateway/presenter integrations, and deployment-specific policy.

## Fixed Core Contracts

| Contract | Fixed abstraction | Core files |
| --- | --- | --- |
| Message patterns | `MessageMeta` models synthetic, hidden, replay-only, no-response, target session/agent, mode, and tags. Concrete Claude Code-style producers remain policy atoms or presenters. | `src/agentm/core/abi/messages.py` |
| Cancellation | `CancelSignal` / `CancelSource` expose a typed reason and carry user cancel, submit interrupt, shutdown, sibling error, and task stop across LLM streams, tool execution, child sessions, and optional `CancellableContextPolicy` transforms. Every session constructs its interrupt and shutdown sources before starting the driver; internal driver inputs require those exact sources rather than silently creating disconnected fallbacks. Provider/compaction streams receive the current turn signal; cancellation during a policy is normalized to the same interrupted-turn outcome instead of an internal failure. Non-cancellable commit and rollback operations settle to a known backend outcome, but a cancellation observed while settling is preserved and propagated after publication or cleanup rather than swallowed; simultaneous cancellation and backend failure preserve both errors. Ownership-producing operations use `settle_known_outcome()` so cleanup can consume the completed result before cancellation propagates. Session shutdown is one shared task: concurrent callers wait for the same driver/event/environment/telemetry/presenter cleanup boundary, and caller cancellation propagates only after that task settles. | `src/agentm/core/abi/cancel.py`, `src/agentm/core/abi/context.py`, `src/agentm/core/runtime/driver.py`, `src/agentm/core/runtime/reaction.py`, `src/agentm/core/runtime/session_core.py`, `src/agentm/core/lib/async_cancel.py` |
| Provider stream | `StreamFn` is the sole provider-neutral asynchronous model boundary. `ThinkingLevel` is part of that public call contract, so providers, retry policies, sessions, and drivers share one ABI type instead of importing or duplicating a runtime-private enum. `OpaqueThinkingBlock` preserves encrypted provider-owned reasoning as immutable JSON for same-provider continuation without pretending that the kernel can inspect it. Adapters preserve every supported semantic field and fail on known unsupported protocol content instead of silently dropping it; transport ids, fingerprints, and log probabilities remain diagnostic metadata outside the trajectory message ABI. | `src/agentm/core/abi/messages.py`, `src/agentm/core/abi/stream.py`, `src/agentm/core/lib/stream.py` |
| Tool execution | `ToolExecutor` and `ToolOrchestrator` model isolation, interrupt behavior, concurrency, sibling cancellation, and completion streaming. `ToolOrchestrator.stream_batch()` yields each terminal result immediately so the runtime can apply result policy and persist a cumulative checkpoint before unrelated parallel tools finish; final provider and trajectory ordering still follows assistant call order. `FunctionTool` can carry validated `ToolExecutionRequirements`; malformed declarations fail instead of degrading to no requirements. `ToolResult` is immutable and restricts extras to portable JSON; result-event replacement is reflected in checkpoints, the committed record, and subsequent decision observation. Orchestration DTO states are validated, and caller task cancellation propagates after child cleanup. Environment routing uses the typed `EnvironmentExecutableTool` adapter rather than tool-name inspection. Process isolation uses a versioned strict JSON result wire and must terminate/reap children on signal or caller cancellation. | `src/agentm/core/abi/tool.py`, `src/agentm/core/abi/tool_executor.py`, `src/agentm/core/abi/tool_orchestration.py`, `src/agentm/core/runtime/tool_orchestration.py`, `src/agentm/execution/wire.py` |
| Permission | `PermissionPolicy.decide()` is async and returns a final `allow` or `deny`. Policies that need user interaction await internally; the runtime has no half-modeled deferred state. | `src/agentm/core/abi/permission.py` |
| Branch/head trajectory model | Committed message nodes and implemented `compact_boundary` control nodes carry `branch_id` and `head_id`; `TrajectoryHead` is the explicit append point for a session/agent/sidechain chain. Node ids are globally unique and session-namespaced. Forks create an empty child head whose `logical_parent_id` points at the selected parent boundary instead of copying parent nodes. Appends never infer "current" from leaves, and a missing head is store corruption rather than a cue to reconstruct a second copy of trajectory state. Node kinds without a producer and transaction contract are not advertised in the ABI. | `src/agentm/core/abi/trajectory.py`, `src/agentm/core/abi/store.py` |
| Store consistency | SDK hosts select exactly one `TrajectoryStore`. The same store owns session metadata, cumulative incomplete `TurnCheckpoint` state, committed `Turn` records, message/control-node indexes, explicit heads, and cache/compaction control state. Every materialized assistant message, individual tool result, and injected message durably updates the incomplete checkpoint before execution advances. `load()`/resume/context expose committed turns only. `commit_turn()` atomically supersedes the matching checkpoint while inserting the committed turn, its message nodes, and the compare-and-advance head update. `commit_compaction()` atomically inserts a compact boundary, its `ContentReplacementState`, and the matching head update. There is no independently configured store pair or rebuildable second trajectory truth. | `src/agentm/core/abi/store.py`, `src/agentm/core/abi/trajectory.py`, `src/agentm/core/runtime/driver.py`, `src/agentm/core/runtime/reaction.py` |
| Portable node indexes | Node query/index fields cover root/session/parent/logical parent/branch/head/agent/sidechain/kind/role/turn/round/message/seq/timestamp, plus logical tool-call, tool-name, cache, content-ref, and visibility lookups. Head indexes are separate. SQL backends materialize these declarations with native B-tree/GIN indexes; scan-oriented stores preserve query semantics without claiming the same physical strategy. | `src/agentm/core/abi/trajectory.py`, `src/agentm/core/abi/store.py`, `src/agentm/storage/trajectory/postgres.py` |
| Async SDK boundary | Store Protocols remain synchronous blocking ports for backend neutrality, while runtime async paths offload store calls with `asyncio.to_thread`. Session creation, checkpoint, turn/compaction commit, cache/replacement-state mutations, environment fork allocation, resource rebinding, and reference resource/catalog backend mutations reach a known backend outcome before caller cancellation propagates. Resource transaction implementations update their local prepare/apply/commit/abandon phase before propagating cancellation, so cleanup cannot race or contradict a still-running filesystem mutation. Ownership-producing operations retain their completed result long enough to commit or abandon it. Low-level core factories perform construction from explicit ports and never search environment/files. The host `AgentSession` presenter resolves one backend from explicit config, environment, TOML, or the project-local JSONL default, and keeps resolver-owned connections alive until the last sharing child/fork shuts down. Optional replaceable objects are considered absent only when they are `None`; a valid falsey Protocol implementation is never replaced by a default. | `src/agentm/sdk.py`, `src/agentm/storage/trajectory/resolve.py`, `src/agentm/storage/resources/local.py`, `src/agentm/storage/catalog/json.py`, `src/agentm/core/lib/async_cancel.py`, `src/agentm/core/lib/tool_executor.py`, `src/agentm/core/runtime/session.py`, `src/agentm/core/runtime/session_core.py`, `src/agentm/core/runtime/session_factory.py`, `src/agentm/core/runtime/driver.py`, `src/agentm/core/runtime/reaction.py` |
| Fork/resume anchors | `TrajectoryForkPoint` can select a turn prefix, node, or head in the source session. `Session.fork()` remains compatible with `TurnRef`; node/head selectors must resolve to the final message of a committed turn or to a control-only compact boundary anchored to one. The resolved anchor preserves both the committed `TurnRef` used by `EffectScope.fork_at()` and the exact node used as the child head's `logical_parent_id`. `EnvironmentForkLease.commit()` promotes provisional fork state into a child-restorable committed boundary before session publication; `abandon()` is idempotent and removes it when publication does not occur. Once child metadata is durable, publication wins cancellation and the environment remains discoverable and resumable. Mid-turn message forks are deliberately unsupported because context projection cannot restore an external world that is snapshotted only at turn commit. `Session.resume(session_id, store, AgentSessionConfig(...))` requires one `TrajectoryStore`, versioned durable context, and complete provider/model/active-set identity for committed history, then runs the full extension/provider factory before resource/environment restoration. Recovery and degraded-readonly activation settle to a known outcome before caller cancellation propagates; any post-construction failure shuts the new session down. Missing identity is rejected rather than inferred from messages or current registration order. There is no constructor-kwargs bypass or node-index rebuild step. | `src/agentm/core/abi/trajectory.py`, `src/agentm/core/abi/compaction.py`, `src/agentm/core/abi/lifecycle.py`, `src/agentm/core/runtime/session.py`, `src/agentm/core/runtime/session_factory.py`, `src/agentm/core/runtime/session_meta.py` |
| Prompt cache state | `PromptCacheState` and `ContentReplacementState` persist cache/replacement identity, branch/head, leaf node, and clone provenance. Cloning preserves the source session/leaf while assigning the target leaf. Enforcement remains a context/provider policy. | `src/agentm/core/abi/trajectory.py`, `src/agentm/core/abi/store.py` |
| Content/reference boundary | Trajectory nodes record control facts such as compaction boundaries, cache identity, replacement identity, and `content_ref`. Large prompt content, summaries, files, and long tool results live behind resource/artifact references rather than inside the trajectory control stream. | `src/agentm/core/abi/trajectory.py`, `src/agentm/core/abi/resource.py` |
| Catalog/trajectory separation | Trajectory queries answer what happened in a session. `AtomCatalog.record_active_set()` persists `CatalogActiveSetInput` with root/parent/session/scenario/provider/timestamp context; `AtomCatalogQuery` answers which atoms, tools, providers, scenarios, and versioned resources were active. Presenter/CLI layers may join them for display, but core keeps the APIs separate because their lifecycles differ. | `src/agentm/core/abi/query.py`, `src/agentm/core/abi/catalog.py` |
| Node-chain context projection | One `ContextProjection` protocol declares its required source as `turns` or `node_chain` and consumes a `ProjectionInput` through `project()`. The input carries committed turns plus, for node-chain policies, the committed chain read from the selected `TrajectoryStore`. Incomplete checkpoints never enter provider context. Projection controls provider input only and does not imply an executable mid-turn fork. | `src/agentm/core/abi/compaction.py`, `src/agentm/core/runtime/reaction.py` |
| Config provenance and provider freeze | `SESSION_CONFIG_PRECEDENCE` and `ConfigValueProvenance` define comparable resolver output. `ProviderSessionIdentity` freezes provider/model identity after the first committed turn; resume restores the lock from session metadata. In-place provider/model changes are not modeled: callers fork or create a new session instead of writing an unimplemented `config_change` control record. | `src/agentm/core/abi/session_api.py`, `src/agentm/core/abi/provider.py`, `src/agentm/core/runtime/session_core.py`, `src/agentm/core/runtime/session_meta.py` |
| Scenario composition boundary | Core session factories create empty SDK sessions from explicit extensions and resolve named scenarios only through host-provided `ScenarioLoader`. Packaged scenario helpers such as `builtin_scenario_loader` live outside `core.runtime`, so embedded SDK users do not inherit CLI/local-tool policy by default. | `src/agentm/core/abi/session_api.py`, `src/agentm/core/runtime/session_factory.py`, `src/agentm/scenarios.py` |
| Scenario-local extension source | `ExtensionSource` represents importable modules or absolute manifest-local files. File sources carry a SHA-256 digest, are resolved as data by the host loader, validated by the runtime §11 gate, loaded under a content-addressed module identity, and retained across catalog freeze, spawn, fork, and resume identity checks. Scenario parsing never preloads synthetic modules into `sys.modules`. | `src/agentm/core/abi/session_api.py`, `src/agentm/scenarios.py`, `src/agentm/core/runtime/extension.py`, `src/agentm/core/runtime/catalog.py`, `src/agentm/core/runtime/session_meta.py` |
| Resource read/write authority | `ResourceWriter` remains the single-mutation workspace-path authority; only `TransactionalResourceWriter` adds atomic turn-scoped prepare/apply/commit/abandon plus resume recovery keyed by typed `ResourceTransactionRef(id, session_id, turn_id, turn_index)` values recorded in `TurnMeta`. There is no generic `batch` API whose atomicity changes by backend. Forked history keeps its parent transaction origins for audit, while resume submits only refs owned by the selected session. `ResourceReader` is the backend-neutral read side for `ResourceRef`; the separately registered `service:resource_store` adds direct logical ref mutation for host-managed content outside a live tool turn and also supplies its read view. Filesystems, object stores, and databases can implement the same summary/artifact/content contract without exposing physical paths. The structured transaction wire is trajectory codec v2; v1 records fail version validation rather than entering a legacy decoder. | `src/agentm/core/abi/resource.py`, `src/agentm/core/abi/_resource_codec.py`, `src/agentm/core/abi/codec.py`, `src/agentm/core/abi/roles.py`, `src/agentm/core/runtime/session.py`, `src/agentm/storage/resources/local.py` |
| Prompt-cache provider bridge | `ProviderPromptCacheAdapter` maps `PromptCacheState` plus provider-bound messages into provider-specific cache hints. The adapter belongs to `ProviderConfig`, so multiple registered providers cannot overwrite one global vendor adapter. A host using a direct `StreamFn` may register one concrete service adapter explicitly. The prompt-cache policy no longer installs a no-op passthrough adapter that falsely implies provider cache support. The reaction runtime invokes the selected adapter immediately before each provider request, rejects cache-identity rewrites, persists returned state, and explicitly copies the selected provider config across lightweight spawn/fork. | `src/agentm/core/abi/provider.py`, `src/agentm/core/abi/roles.py`, `src/agentm/core/runtime/reaction.py`, `src/agentm/core/runtime/session.py` |
| Effect/world transaction boundary | `EffectScope` snapshots the external world around each turn and must exclude trajectory logs, resource journals, catalogs, snapshot metadata, and other control-plane persistence owned by independent commit participants. The driver prepares world/resource participants, atomically commits the turn plus its node/head indexes in the selected trajectory store, commits the resource and effect participants, and only then publishes the turn. | `src/agentm/core/abi/lifecycle.py`, `src/agentm/core/runtime/driver.py` |
| Environment restore failure | Resume is fail-fast by default. A host may continue only by registering `EnvironmentRestoreFailureHandler`, whose `activate_degraded_readonly()` must enforce read-only behavior before the runtime records `EnvironmentRestoreStatus(state="degraded_readonly")`. | `src/agentm/core/abi/lifecycle.py`, `src/agentm/core/runtime/session.py` |
| Tool/environment composition | `ToolExecutionRequirements` can target an environment id; `ToolExecutionCapabilities` and `ToolExecutionRequest` carry `EnvironmentRef`. `ToolExecutor` owns call isolation/killability/concurrency while `EnvironmentOperations` owns environment identity and world operations. SDK hosts inject an external backend explicitly through `AgentSessionConfig.environment_operations`; the paired environment/bash services inherit with resource scope across ordinary children and are explicitly replaceable for a different child/fork environment. Atom-owned defaults remain session-scoped, and accidental duplicate atom registration still fails. | `src/agentm/core/abi/tool_executor.py`, `src/agentm/core/abi/operations.py`, `src/agentm/core/abi/session_api.py`, `src/agentm/core/lib/tool_executor.py`, `src/agentm/core/runtime/session_core.py`, `src/agentm/core/runtime/session_factory.py` |
| Capability dependency validation | Manifest dependencies resolve only against explicit normalized capability keys such as `service:operations`, `tool:bash`, and `provider:openai`. Bare requirements and unknown capability kinds are validation errors rather than compatibility aliases. `ExtensionManifest` rejects unknown fields so obsolete declarations cannot appear effective while being silently ignored. | `src/agentm/core/abi/manifest.py`, `src/agentm/core/runtime/session_factory.py` |
| File toolbox/resource boundary | `agentm_toolbox` is one I/O-free implementation of file rendering, read-before-mutation state, stale-read detection, and edit planning for every environment. The atom obtains current bytes and applies planned bytes only through the active `ResourceTxn` or host `ResourceWriter`; local and remote environments replace that raw resource port, never the toolbox policy. Ordered exact/quote/whitespace text-equivalence matching remains one deterministic planner algorithm and still enforces unique/stale checks. There is no bash/CLI fallback, target-environment installation, or second state file that can bypass turn transactions, constitution checks, or recovery. | `src/agentm/extensions/builtin/file_tools.py`, `src/agentm_toolbox/` |
| Observability implementation boundary | Core exposes only `SessionTelemetry` and query Protocols. Generic strict OTLP parsing and query adapters live in `agentm.observability`; OTel SDK exporters, collector discovery, local JSONL writers, and Loguru bridging remain extension/backend details. Sink selection is explicit through `export = auto | local_file | otlp`: only `auto` may select local diagnostics when no collector is available, while explicit `otlp` fails session setup if its sink cannot attach. | `src/agentm/core/abi/telemetry.py`, `src/agentm/core/abi/query.py`, `src/agentm/observability/`, `src/agentm/extensions/builtin/observability.py`, `src/agentm/extensions/observability/` |
| Gateway wire and host ports | The v2 wire uses frozen, versioned `GatewayEnvelope` DTOs with explicit `durable | ephemeral` delivery. `OutboxStore`, `InboxLog`, `Authenticator`, and `SessionBindingStore` are transport/backend-neutral async ports; transport credentials and presenter payloads are typed and runtime validated. Core runtime has no gateway dependency. | `src/agentm/gateway/wire.py`, `src/agentm/gateway/ports.py`, `src/agentm/gateway/forwarding.py` |

## Recovery Boundaries

| Path | Behavior | Reason |
| --- | --- | --- |
| Missing trajectory head | Fail fast before the turn commit. | The selected store creates and advances heads transactionally with session/turn records. Rebuilding a second projection would hide corruption and reintroduce two sources of truth. |
| Atomic trajectory commit failure | Treat the turn, its committed message nodes, and its head advance as uncommitted; restore the effect world first, then abandon the resource transaction. | `commit_turn()` is the only trajectory publication boundary. A backend cannot report success for the turn while omitting its query/index state. |
| Authoritative append failure after resource apply/effect prepare | Restore the effect world first, then abandon the resource transaction and its journal; collect both cleanup failures without running them concurrently. | Both participants may touch the same workspace. Concurrent rollback creates a filesystem race and can turn a determinate append failure into an indeterminate world state. |
| Effect snapshot contains SDK control-plane state | Backend composition is invalid. Snapshot implementations must exclude all independently durable SDK stores; the local reference backend excludes `.agentm` by default. | Resume runs each store's recovery before restoring the world. Restoring a captured resource journal or trajectory log would rewind a commit that already reached a known outcome. |
| Node-chain projection without store/head | Fail fast when a `ContextProjection` declares `source = "node_chain"` but no selected `TrajectoryStore` or active head can supply its committed chain. | Exact replay must not silently widen back to turn replay. |
| Node/head selector targets a mid-turn message | Reject the fork even when exact node-chain projection is installed. | Projection can reconstruct provider context, but only committed turn boundaries have matching `EffectScope` snapshots and a complete execution outcome. |
| Compaction boundary/state publication | Persist the immutable summary resource first, then atomically publish the `compact_boundary`, matching `ContentReplacementState`, and head advance through `commit_compaction()`. | A visible boundary without its interpretation state would make replay and fork nondeterministic; an orphan immutable summary is harmless after a failed store commit. |
| Compaction summary/state mismatch | Write the immutable summary `ResourceRef` before saving `ContentReplacementState`. Missing summary content, invalid coverage, or an upstream projection that changes the uncompacted turn suffix fails fast; no implicit tail-only fallback is used. | Compaction replaces model-visible history, so a partial or silently widened recovery would change semantics. Orphaned immutable summaries are harmless and can be collected separately. |
| Environment restore failure on resume | Raise `EnvironmentRestoreError` unless a registered `EnvironmentRestoreFailureHandler` successfully activates degraded read-only enforcement. | External world state is part of trajectory correctness; continuing after failed restore requires an enforceable host action, not a passive policy flag. |
| Environment fork is cancelled or child construction fails | Wait for the backend fork/rebind operation to settle. Commit the provisional lease before child metadata publication; if no child metadata became durable, shut down any constructed runtime and idempotently abandon the lease. If metadata is durable, retain the committed environment for explicit resume. | Cancelling an `asyncio.to_thread` awaiter does not stop allocation. Deleting state after durable child publication would corrupt a valid branch, while retaining state without publication would leak an unreachable environment. |
| Provider selection before first commit | An explicit `ProviderResolver` may choose the initial provider while no history exists. Once history commits, persisted `ProviderSessionIdentity` pins provider/model/active-set identity; resume never guesses a missing identity from messages, model names, or provider registration order. | Provider selection is policy before history; after history, silent model drift corrupts replay semantics. |
| Provider request raises after retries | Commit a `ProviderRequestFailed` turn, including the provider exception type, detail, and partial-event count, before failing the trigger receipt. The turn and its node messages remain queryable but are marked non-replayable. | A session header without the failed request is not an auditable trajectory. Replaying an uncertain partial request into a later provider call would also be incorrect. |
| Process exits with an incomplete turn | Retain the latest `TurnCheckpoint` in the selected trajectory store and expose it through trajectory queries. Resume, fork prefixes, provider context, compaction, and committed message-index queries ignore it because it has no committed `Outcome`. After identity, resource, and environment recovery succeeds, the SDK host compare-and-discards the exact observed orphan checkpoint before accepting a new turn. A missing checkpoint is idempotent; any changed snapshot fails rather than erasing concurrent work. | Materialized messages remain inspectable after a crash without replaying an uncertain tool call or provider step. Explicit compare-and-discard lets execution continue from the committed prefix without fabricating a termination cause or silently overwriting another incomplete turn. |

## Store Backend Contract

Every configured trajectory backend implements one `TrajectoryStore`. A
backend may use separate physical tables or indexes, but those are private
implementation details behind one lifecycle and transaction boundary. SDK
configuration never exposes separately selectable turn and node stores.

Required node identity and ordering fields:

- `id`
- `session_id`
- `root_session_id`
- `parent_session_id`
- `seq`
- `parent_id`
- `logical_parent_id`
- `branch_id`
- `head_id`

Required turn/message join fields:

- `turn_id`
- `turn_index`
- `round_index`
- `message_index`
- `kind`
- `role`
- `visibility`
- `timestamp`

Required routing/query fields:

- `agent_id`
- `is_sidechain`
- `tool_call_id`
- `tool_name`
- `cache_key`
- `content_ref`

The `tool_call_id` and `tool_name` fields are logical inverted indexes. A
message node may contain multiple tool calls/results; SQL stores may normalize
them into a child table, while JSONL stores may scan arrays.

Head storage is separate from node storage. A backend must support a unique
current head per `(session_id, head_id)` and efficient branch/agent/sidechain
selection by `(root_session_id, session_id, branch_id, agent_id, is_sidechain)`.

## Implementation Status

| Capability | Design decision | Status | Implemented protocol / behavior | Home |
| --- | --- | --- | --- | --- |
| Durable trajectory backends | A session selects one trajectory backend: JSONL for local portability or PostgreSQL for durable SQL storage. ClickHouse is not a trajectory backend. | Implemented. | Memory, JSONL, and PostgreSQL persist session/checkpoint/turn/node/head/cache/content state behind one `TrajectoryStore`. Concrete backends live under `agentm.storage` and depend only on core ABI/lib contracts; JSONL replays its journal through the same in-memory reference state without importing runtime. Backend-neutral validation requires contiguous node sequence, one physical append chain, and exact node/head branch, agent, sidechain, and lineage identity before publication. Session-scoped methods distinguish an unknown session (`KeyError`) from a missing head or policy state (`None`); cross-session chain starts are accepted only through an empty head's explicit logical parent. Cross-session queries use portable `(session_id, seq)` ordering. The in-memory reference serializes complete operations; JSONL writes one fsync-backed journal record per mutation; PostgreSQL enforces session foreign keys and updates each turn, compaction, state clone, and head mutation in one connection transaction. | `src/agentm/core/abi/store.py`, `src/agentm/core/lib/trajectory_store.py`, `src/agentm/core/lib/trajectory_nodes.py`, `src/agentm/storage/trajectory/memory.py`, `src/agentm/storage/trajectory/jsonl.py`, `src/agentm/storage/trajectory/postgres.py` |
| Incremental turn durability | Every backend enforces one contiguous committed turn sequence with unique turn ids per session, plus at most one cumulative checkpoint for the next index. Each materialized step updates that checkpoint before execution advances; configured durable backends fail instead of silently changing storage. | Implemented. | `load()` filters incomplete state while `TrajectoryQueryStore.checkpoints()` keeps the latest cumulative incomplete turn inspectable. `ToolOrchestrator.stream_batch()` exposes each individual completion; the runtime applies `ToolResultEvent` policy and saves the cumulative pending round before waiting for unrelated tools. | `src/agentm/core/lib/trajectory_store.py`, `src/agentm/core/runtime/reaction.py`, `src/agentm/core/runtime/tool_orchestration.py`, `src/agentm/core/runtime/stores/query.py`, `src/agentm/cli/_trace.py` |
| Host trajectory backend selection | Public SDK configuration selects one caller-owned `trajectory_store`. Otherwise `AgentSession` applies strict precedence `AGENTM_TRAJECTORY_DSN`/`AGENTM_TRAJECTORY_DIR` > project `agentm.toml` > user config > project-local JSONL; conflicting, malformed, or unknown configuration fails. Resolver-owned PostgreSQL connections are shared by descendants and close after the final session. | Implemented. | Core factories receive only the explicit store and contain no backend discovery. Trace CLI uses the same selected store and closes resolver-owned query resources after each command. | `src/agentm/sdk.py`, `src/agentm/storage/trajectory/resolve.py`, `src/agentm/cli/_store.py`, `src/agentm/cli/_trace.py`, `src/agentm/core/runtime/session_factory.py` |
| Exact node-chain context replay | Exact replay is one `ContextProjection` implementation with `source = "node_chain"`; cache/content replacement stays layered policy. | Implemented. | `ExactNodeChainProjection.project()` replays `ProjectionInput.nodes`, preserves cache/content refs, supports compact-boundary metadata-only refs, sidechain filtering, and `ProjectionReport`. | `src/agentm/extensions/builtin/context_projection.py` |
| Prompt-cache/content-replacement policy | Cache/replacement are trajectory control facts; large payloads stay behind `content_ref`. | Implemented. | `PromptCacheContextPolicy` tags provider context with stable cache/content refs and persists `PromptCacheState`; `OpenAIPromptCacheAdapter` materializes the selected provider request as `prompt_cache_key` plus `prompt_cache_retention`. Direct streams receive cache syntax only from an explicitly registered concrete adapter. | `src/agentm/extensions/builtin/prompt_cache.py`, `src/agentm/extensions/builtin/llm_openai.py` |
| Provider-owned opaque reasoning | Provider-neutral messages distinguish visible `ThinkingBlock` content from encrypted reasoning that only its owning provider can replay. | Implemented and tested. | `OpaqueThinkingBlock` freezes one provider id and JSON payload. Anthropic maps `redacted_thinking` into this block and emits it unchanged on the next request; OpenAI rejects it. Anthropic rejects unsupported content blocks, citation deltas, and unknown stream events. OpenAI preserves refusal text and rejects multiple choices, non-assistant deltas, deprecated `function_call`, and non-function tool calls instead of losing semantic content under a compatibility path. | `src/agentm/core/abi/messages.py`, `src/agentm/core/abi/codec.py`, `src/agentm/core/lib/stream.py`, `src/agentm/extensions/builtin/llm_anthropic.py`, `src/agentm/extensions/builtin/llm_openai.py`, `tests/integration/test_provider_stream_behaviors.py` |
| Claude Code message-pattern policy atoms | Core owns metadata/cancel invariants; concrete producers are atom/presenter/gateway policy. | Implemented. | `MessagePatternFactory` models interrupt, missing tool results, permission rejection, plan rejection, task notification XML, local command caveats, attachment/reminder messages, teammate/channel messages, and no-response messages; optional policy injects configured reminders. | `src/agentm/extensions/builtin/message_patterns.py` |
| Fork/resume/cache/compaction/failure E2E coverage | Tests operate as an SDK user through `AgentSession.create/resume/fork/run/interrupt/shutdown`; the provider is stubbed only at `StreamFn`, and assertions target provider-visible requests, persisted public store records, and returned transcripts rather than runtime internals. | Implemented. | The same contracts run against one local JSONL store or one real PostgreSQL store. They cover cumulative crash checkpoints, node/head fork selectors, rejection of arbitrary cross-session and mid-turn anchors, explicit inherited logical-parent chains, portable cross-session query order, unknown-session fail-stop behavior, fork cancellation lease abandonment, immediate resume of an isolated environment fork, parent/child resource-transaction ownership, persisted compact boundaries, compaction inheritance, and atomic final turn publication. | `tests/integration/test_sdk_trajectory_behaviors.py`, `tests/test_lifecycle_catalog_ports.py`, `tests/fixtures/prompt_cache_adapter.py` |
| Durable catalog backend | Catalog identity stays separate from trajectory event order. | Implemented. | `JsonCatalogStore` implements `AtomCatalog`, `AtomCatalogQuery`, and `VersionedResourceStore` with durable active-set records, resource versions, aliases, and content-addressed payload files. | `src/agentm/storage/catalog/json.py` |
| Indexed catalog query | Catalog query remains separate from `TrajectoryQueryStore`; UI/CLI may join at presentation time. | Implemented. | `JsonCatalogStore.query_active_sets()` supports session/root/parent/scenario/provider, atom name, module path, digest, version, requirements, and provided capability predicates. | `src/agentm/storage/catalog/json.py` |
| Remote observability query | Exactly one selected `TrajectoryStore` owns complete session/turn data. OTLP is an optional diagnostic transport and never a persistence fallback; ClickHouse reads only collector-managed `otel_logs`/`otel_traces`. Correlation uses stable session/root/parent/turn ids without copying trajectory records. | Implemented. | `OtlpJsonlQueryStore` and `ClickHouseObservabilityQueryStore` implement only `ObservabilityQueryStore`. `CompositeTraceQueryStore` presents one `TraceQueryStore` by delegating session/turn reads to the selected JSONL/Postgres trajectory query source and event/span reads to one observability source; it performs no replication or fallback. | `src/agentm/observability/query.py`, `src/agentm/storage/query/clickhouse.py`, `src/agentm/storage/query/composite.py` |
| Concrete `SessionSpecResolver` | Precedence is explicit SDK fields, atom overrides, env, project config, user config/profile, scenario defaults, provider defaults. | Implemented. | `DefaultSessionSpecResolver` resolves scenario loader outputs, extensions, atom config, provider profiles/env, and emits `ResolvedSessionSpec.value_provenance` fingerprints. | `src/agentm/config/resolver.py` |
| Provider/session config changes | Provider/model identity freezes after first commit; changes require fork or a new session. | Implemented as a fail-stop invariant. | Session provider selection validates the frozen `ProviderSessionIdentity`; resume validates persisted provider/model/active-set identity. No in-place config-change protocol or fictitious control-node producer is advertised. | `src/agentm/core/runtime/session_core.py`, `src/agentm/core/runtime/session_meta.py` |
| Resource/artifact backends | One `ResourceRef` namespace model maps logical resources to backend storage; turn transactions reconcile against typed, origin-scoped committed transaction refs from the authoritative trajectory. | Implemented. | `LocalResourceStore` implements `ResourceStore`, `ResourceWriter`, recoverable prepare/apply/commit/abandon transactions, workspace/artifact/sandbox/summary/content/catalog/observability/environment namespaces, path containment, and immutable validated mutation records. Forked histories retain parent mutation evidence without asking a child writer to recover parent journals. SQL/object-store implementations can expose logical ref methods without workspace path semantics. | `src/agentm/core/abi/resource.py`, `src/agentm/core/abi/_resource_codec.py`, `src/agentm/storage/resources/local.py` |
| Sandbox/remote environment backend | Remote execution belongs in `EnvironmentOperations`; tool isolation composes through `ToolExecutor`. | Implemented with local and Harbor backends. | `LocalEnvironmentOperations`, `LocalSnapshotStore`, and `LocalSnapshotEffectScope` provide environment identity, local bash reuse, copy-based snapshots, fork, restore, and lifecycle hooks. The Harbor host adapter injects `HarborEnvironmentOperations` and `HarborResourceWriter` directly, honors remote cwd/stdin/streaming, and terminates/reaps marked remote process groups on signal or caller cancellation without thread-local or atom-global environment state. | `src/agentm/environments/local.py`, `contrib/scenarios/harbor/src/agentm_harbor/harbor_ops.py`, `contrib/scenarios/harbor/src/agentm_harbor/external_agent.py` |
| Environment snapshot persistence | Resume restore fails fast unless a host supplies an enforceable degraded-read-only handler; control-plane persistence is outside the snapshot world. | Implemented as reference backend. | Copy-based snapshot persistence records validated immutable `EnvironmentSnapshot` manifests and restores/forks via `EnvironmentSnapshotter` and `EffectScope`. Every fork returns an `EnvironmentForkLease`: local commit atomically promotes `fork` to the child `after_turn` checkpoint, while abandon removes both the materialized workspace and provisional snapshot. `LocalSnapshotStore` excludes `.agentm` by default, while custom in-workspace control roots must be declared through `excluded_names`. | `src/agentm/core/abi/lifecycle.py`, `src/agentm/environments/local.py` |
| Process/sandbox `ToolExecutor` | `ToolExecutor` owns isolation/killability/concurrency; `EnvironmentOperations` owns environment world effects. | Implemented. | `SandboxToolExecutor` invokes only tools implementing `EnvironmentExecutableTool` for environment isolation and fails rather than running unsupported tools on the host. `ProcessToolExecutor` runs explicit `metadata['process_entrypoint']` tools, exchanges only strict versioned JSON DTOs, and terminates/reaps subprocesses on signal or caller cancellation. | `src/agentm/execution/sandbox.py`, `src/agentm/execution/process.py`, `src/agentm/execution/wire.py`, `src/agentm/execution/worker.py` |
| Detached tool execution | Auto-backgrounding is an execution policy and composes as an outer `ToolExecutor`, not by mutating registered tool objects. A tool inherits the turn signal only during its foreground phase; timeout transfers cancellation ownership to the background atom, so parent interruption cannot kill detached work. Explicit task stop and session shutdown remain enforceable. | Implemented and tested. | `background_exec` preserves fast results, returns a task ticket after timeout, delivers the eventual outcome through `BackgroundCompletion`, propagates terminal outcomes, and exposes typed check/cancel companion tools. SDK behavior coverage interrupts the parent request after detachment and verifies the tool still completes. | `src/agentm/extensions/builtin/background_exec.py`, `tests/integration/test_sdk_trajectory_behaviors.py` |
| Child-agent dispatch | Foreground and background children have explicit cancellation ownership. Foreground dispatch inherits the parent signal and returns through the tool call. Background dispatch uses an independent child signal, returns a task ticket, and later publishes a non-terminal `SubagentResult`; parent interruption cannot stop it, while task stop and session shutdown can. `background_exec` bypasses `dispatch_agent` so only this policy chooses the child domain. | Implemented and tested. | `sub_agent` uses only `SpawnedSession`, `EventCancelSource`, child lifecycle events, and the trigger queue. Spawn metadata becomes durable before live handoff; cancellation during handoff therefore finishes graph registration and emits a paired child start/end lifecycle before propagating. Failed or cancelled child construction releases its reserved worker slot before surfacing the outcome. A trajectory fork records durable lineage but emits no live-child event because it may branch from an already closed source session. Instruction injection uses a targeted immediate child trigger; no child internals, physical artifact scan, persona reflection, duplicate lifecycle event, or gateway-private registry is retained. The SDK behavior contract interrupts the parent after background dispatch and verifies the child completes and its result reaches the parent model. | `src/agentm/core/runtime/session.py`, `src/agentm/extensions/builtin/sub_agent.py`, `src/agentm/extensions/builtin/background_exec.py`, `tests/integration/test_sdk_trajectory_behaviors.py`, `tests/test_lifecycle_catalog_ports.py` |
| Legacy atom retention | An atom or service alias is retained only when a current scenario or documented SDK entry point can execute its complete contract. Registration-only stubs, no-op adapters, references to removed modules, direct filesystem emulation of a typed store, and unselected policy implementations are deleted rather than advertised as compatibility. Historical implementations remain available through Git history; future functionality must return as an implementation of the fixed ABI ports. | Enforced. | The accidentally restored workflow/artifact/prompt/slash/supervision paths and unselected file-queue, loop-budget, permission, runtime-context, skill-loader, structured-output, bash-guard, tool-filter, and TUI policy atoms were removed. Registration-only session-spec-resolver and unused observability-query aliases were removed; callers use `AgentSessionConfig.spec_resolver` and the independent `ObservabilityQueryStore` port directly. Harbor environment and interrupt bindings are host/presenter code rather than atoms, eliminating ambient thread-local injection and ignored manifest metadata. Unknown manifest fields now fail validation instead of silently creating fake compatibility. Memory list/lookup/frontmatter failures no longer masquerade as empty or missing state; search surfaces per-file skips to the model and index rebuild fails before replacing the existing index. Only non-authoritative access statistics remain explicitly non-fatal. | `src/agentm/core/abi/manifest.py`, `src/agentm/core/abi/roles.py`, `src/agentm/core/abi/session_api.py`, `src/agentm/core/abi/resource.py`, `src/agentm/core/abi/context.py`, `src/agentm/core/abi/trigger.py`, `src/agentm/core/abi/permission.py`, `src/agentm/core/abi/tool_executor.py`, `src/agentm/extensions/builtin/memory.py`, `contrib/scenarios/harbor/src/agentm_harbor/` |
| Context projection and durable LLM compaction | Summaries/payloads live behind `content_ref`; trajectory records replacement identity and coverage. | Implemented. | `ExactNodeChainProjection` handles exact replay; `TailContextProjection` provides explainable tail projection; `LlmCompactionPolicy` asynchronously summarizes complete turn prefixes, writes immutable summary refs, persists/reuses `ContentReplacementState`, incrementally extends summaries, and inherits fork state only on exact logical-leaf matches. | `src/agentm/extensions/builtin/context_projection.py`, `src/agentm/extensions/builtin/llm_compaction.py` |
| Capability dependency validation | Dependencies target explicit capability keys; no bare-name compatibility path remains. | Implemented and enforced. | `parse_capability_ref()` and `requirement_key()` reject bare requirements and unknown capability kinds on the actual load-time dependency-resolution path. The constitution-listed source validator is required at runtime and has no optional-import or unused authoring-wrapper path. | `src/agentm/core/abi/manifest.py`, `src/agentm/core/runtime/session_factory.py`, `src/agentm/core/runtime/extension.py`, `src/agentm/extensions/validate.py` |
| Minimal SDK baseline packaging | Core baseline remains scenario-neutral with optional provider/storage/query extras. | Implemented. | Provider/OTel/tokenizer remain optional; storage extras now advertise Postgres and ClickHouse adapters; packaged scenario helpers stay outside `core.runtime`. | `pyproject.toml`, `uv.lock`, `src/agentm/scenarios.py` |
| Presenter/gateway/authoring capabilities | Presenter, gateway, and authoring workflows stay outside minimal core. | Protocol layer implemented. | Authoring owns strict YAML-frontmatter artifacts and filesystem output; it never imports presenter code. Gateway defines strict wire DTOs plus pluggable outbox, inbox-idempotency, authentication, session-binding, and child-forwarding ports. The minimal SDK intentionally supplies no default peer server or gateway persistence backend; concrete hosts implement these ports without reopening core. | `src/agentm/presenter/`, `src/agentm/gateway/`, `src/agentm/authoring/` |
| Interactive CLI and contrib scenario loading | Presenter code consumes `ScenarioLoader`; core runtime performs no source-tree search and injects no default atoms. A source-tree scenario is advertised only when every selected atom is part of that executable composition; deployment packages own their private manifests instead of duplicating them under the CLI scenario root. | Implemented and tested. | The CLI package exposes chat/run/config/scenario commands and manifest-local atoms are durable file sources. Config inspection recursively redacts credentials. Default atom composition remains in scenario manifests rather than the runtime factory. The SDK behavior contract creates every discoverable packaged scenario, preventing stale config fields and references to removed or optional atoms. The Harbor adapter packages one explicit-path manifest, injects host services before composition, and owns a presenter socket that uses `prompt(priority="now")` for real `submit_interrupt` cancellation plus queued feedback. Its CLI waits for delivery acknowledgement. Obsolete standalone ARL overlays and duplicated toolbox wheel contents were removed rather than retained as broken compatibility routes. | `src/agentm/cli/`, `src/agentm/scenarios.py`, `src/agentm/core/lib/redact.py`, `contrib/scenarios/chat/manifest.yaml`, `contrib/scenarios/interrupt_demo/manifest.yaml`, `contrib/scenarios/harbor/scenario.yaml`, `contrib/scenarios/harbor/src/agentm_harbor/`, `tests/integration/test_sdk_trajectory_behaviors.py`, `tests/integration/test_harbor_adapter.py` |
| Composition-boundary code-health gate | Architectural constraints that Ruff and mypy cannot express are AST-scanned without importing inspected modules. | Tested. | `AM001`-`AM014` retain the main-branch exception, shape, layering, schema, timeout, and path guards; `AM010` now also rejects core-to-extension/backend and storage/environment/execution/extension-to-runtime dependency inversions. The constitution-listed extension validator is the sole precise core-to-extension exception. Obsolete mutable-event `AM015`/`AM016` are retired because v2 events are frozen DTOs with typed control returns. `AM017` rejects concrete builtin atoms in core runtime; `AM018` rejects scenario-time Python execution and `sys.modules` mutation; `AM019` rejects legacy tuple/index access to canonical extension specs; `AM020` rejects raw extension config materialization in CLI output; `AM021` rejects `getattr`/`hasattr`/`setattr`/`delattr`; `AM022` rejects `Any` across all scanned source; `AM023` rejects unparameterized `dict` annotations across all scanned source; `AM024` rejects standard-library `logging` imports so all project logging uses Loguru. Precise `code-health: ignore[...]`, `ignore-file[...]`, `type: ignore`, and `noqa` markers may exempt a real dynamic boundary without disabling unrelated rules. The gate is available as `python -m agentm.code_health` and `agentm lint`. | `src/agentm/code_health.py`, `tests/test_code_health.py` |

## Verification Gates

| Gate | Result |
| --- | --- |
| Static lint | `uv run ruff check src/agentm` passes. |
| Static types | `uv run mypy src/agentm` passes. |
| Unit/integration suite | `uv run pytest --tb=short` passes, including SDK-user resume/fork/interrupt behavior contracts with a stub provider; `uv run --extra harbor pytest tests/integration/test_harbor_adapter.py` additionally verifies host injection, child inheritance, stdin/cwd, cancellation/reaping, and presenter acknowledgement. |
| Backend smoke | JSONL crash-tail recovery and one-journal session isolation pass. |
| Docker backend smoke | `tools/otel/docker-compose.yaml` brings up healthy ClickHouse, OTel collector, and Postgres. Real Postgres runs 33 SDK trajectory behavior contracts through one store/connection transaction boundary; a real SDK session keeps its complete trajectory in the selected store while OTLP events/spans are queried from collector-managed ClickHouse tables. |
| SDK behavior E2E | JSONL runs by default; setting `AGENTM_TEST_POSTGRES_URL` runs the identical resume/fork/cache/compaction/checkpoint contracts against one PostgreSQL store. Setting `AGENTM_TEST_CLICKHOUSE_URL` plus `AGENTM_TEST_OTLP_ENDPOINT` verifies the independent OTLP observability plane and read-side composition without trajectory replication. |
