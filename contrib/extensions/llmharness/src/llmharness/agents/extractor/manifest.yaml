name: extractor
description: >
  Cognitive audit extractor: incremental event/edge graph builder.
  Walks new turn windows and maintains a logic-flow graph via
  upsert/delete/finalize tools.

extensions:
  - module: agentm.extensions.builtin.observability
  - module: agentm.extensions.builtin.operations
  - module: llmharness.audit.extractor.atom
  - module: agentm.extensions.builtin.system_prompt
    config:
      prompt: ""
