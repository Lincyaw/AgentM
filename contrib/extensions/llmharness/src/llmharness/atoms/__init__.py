"""AgentM atom modules exposed by the llmharness package.

Each submodule is a §11 single-file extension with module-level ``MANIFEST``
and ``install(api, config)``. Atoms here are the bridge between the
llmharness Python package (schema, store, cards loader) and AgentM
scenarios such as ``harness_monitor``.
"""
