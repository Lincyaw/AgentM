name: pattern_miner
description: |
  Offline failure-pattern miner. One session reviews one case
  (task, trajectory, eval evidence) along one feedback dimension and
  submits structured findings. The dimension model lives in
  docs/policy-feedback-dimensions.md. Agent definition: agents/miner.yaml;
  angle prompts: prompts/miner/.

includes:
  - agents/miner.yaml
