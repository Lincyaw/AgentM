name: rca:harness.sync.opinions
description: |
  Sync audit, opinions-only: auditor fires but reminders are NOT injected.
  The investigator trajectory is identical to rca:baseline.
task_class: rca_baseline

includes:
  - overlays/base-investigator.yaml
  - overlays/runtime-context.yaml
  - overlays/harness-sync.yaml
