name: rca:harness.sync.opinions10
description: |
  Opinions-only at 10-turn cadence; for chained-fork intervention experiments.
task_class: rca_baseline

includes:
  - overlays/base-investigator.yaml
  - overlays/runtime-context.yaml

extensions:
  - module: llmharness.adapter
    config:
      mode: sync
      extractor_interval_turns: 10
      audit_interval_turns: 10
      enable_reminders: false
