id: 02_pod_failure
task_class: rca_baseline
# Held out from primary score so the gate can't overfit a single fault topology.
holdout: true
description: |
  PodFailure on ts-contacts-service. Single-service fault, no propagation.
  Source: ts0-ts-contacts-service-pod-failure-j42hd8.
input:
  user_message: |
    The following API endpoints are experiencing possible SLO violations and need investigation:
    - HTTP GET http://ts-ui-dashboard:8080/api/v1/contactservice/contacts/account/{accountId}

    Please investigate the root cause of these SLO violations.
  fixtures:
    - /home/ddq/AoyangSpace/dataset/rca/ts0-ts-contacts-service-pod-failure-j42hd8
expected:
  expected_services:
    - ts-contacts-service
  fault_kind: pod_failure
budget:
  max_turns: 25
  max_cost_usd: 0.30
