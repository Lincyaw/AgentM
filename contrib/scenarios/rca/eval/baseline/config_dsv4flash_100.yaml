# DSv4flash baseline — 100 ops-lite cases (same slice as ab100).
#
# Run:
#   uv run rca llm-eval run contrib/scenarios/rca/eval/baseline/config_dsv4flash_100.yaml \
#     -a agentm -l 100 -n 8 --ak scenario=rca:baseline --ak model=litellm-dsv4flash

exp_id: "agentm-dsv4flash-baseline-100-0604"
agent_type: "agentm"
model_name: "DeepSeek-V4-flash"

data:
  dataset: "ops-lite"
  type: "single"
  question_field: "question"
  gt_field: "answer"
  tags: ["ops-lite"]

source_path: "/home/ddq/AoyangSpace/AgentM/datasets/ops-lite/cases"
source_path_pattern: "{source_path}/{source}"

concurrency: 8
judge_concurrency: 8

judge_model:
  model_provider:
    type: "chat.completions"
    model: "DeepSeek-V4-flash"
    api_format: "openai"
    base_url: "http://exp-remote:8088/v1"
    extra_query:
      warpgate-ticket: "fd3a3506c693c83e1522e2a01155cac182764795b176e96b7ceae557a49e56fd"
    verify_ssl: false
