name: arl:harness_minimal
description: Sync llmharness auditor — minimal prompt, no methodology.

includes:
  - overlays/base.yaml

extensions:
  - module: llmharness.atom
    config:
      mode: sync
      audit_interval_turns: 5
      auditor_prompt: swe
      enable_reminders: true
      enable_methodology: false
