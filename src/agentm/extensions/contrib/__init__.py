"""Community / ported extension atoms.

Each ``.py`` file under this package follows the same single-file §11
contract as ``agentm.extensions.builtin``. The ``contrib`` prefix signals
that these atoms originate outside the core catalog (community ports,
vendor tool mirrors, etc.) and may carry different stability expectations.
"""
