{
  "mcpServers": {
    "vault": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run",
        "vault-mcp"
      ],
      "env": {
        "VAULT_DIR": "./vault",
        "VAULT_EMBEDDING_MODEL": "sentence-transformers/all-MiniLM-L6-v2"
      }
    }
  }
}